server:
  port: 8080

spring:
  application:
    name: climate-risk-stress-test
  datasource:
    url: jdbc:mysql://localhost:3307/climate_risk_stress?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: crst
    password: crst123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

# 主系统登录/权限/日志由平台承接，本模块仅透传当前用户标识（开发态 mock）
app:
  mock-user-id: HQ_ADMIN_001
  mock-user-name: 总行管理员
