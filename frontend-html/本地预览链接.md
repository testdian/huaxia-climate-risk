# 气候风险压测 — HTML 原型本地预览

## 启动方式

在终端执行（需在 `frontend-html` 目录下）：

```bash
cd "/Users/fangdanyang/Desktop/HUAXIA BANK-CLIMATE RISK/climate-risk-stress-test/frontend-html"
python3 -m http.server 8765
```

保持该终端窗口不要关闭。若 8765 端口已被占用，可改成其他端口，例如 `8888`，并把下面链接里的 `8765` 一并替换。

## 访问链接

| 说明 | 链接 |
|------|------|
| **完整版（推荐）** — 华夏银行绿金气候风险压测，含任务五步流程、配置、分页等 | http://localhost:8765/index-full.html |
| 简化版 — 早期演示页 | http://localhost:8765/index.html |

## 快捷复制

**完整版：**

```
http://localhost:8765/index-full.html
```

**简化版：**

```
http://localhost:8765/index.html
```

## 说明

- 纯静态页面，无需安装 Node / 后端即可预览。
- 数据为浏览器内 Mock（`js/store-full.js`），刷新页面会恢复初始演示数据。
- E2E 测试默认也使用 `http://localhost:8765` 作为基址。
